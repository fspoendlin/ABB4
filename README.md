# ABB4: Prediction of conformational ensembles of antibodies


## install
```bash
bash scripts/install.bash
```

## predict
change csv pat, batch size and num samples in abb4/configs/interpolant.yaml, for batch size start with GPU memory (in GB) / 0.5

```bash
bash scripts/interence.bash
```
